name: weekday_selector_example
description: A new Flutter project.
version: 1.0.0-nullsafety.0
publish_to: none
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  # Used for supporting multiple languages
  intl: any
  flutter_localizations:
    sdk: flutter
  # The package
  weekday_selector:
    path: ../
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
